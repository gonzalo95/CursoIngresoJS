function Mostrar()
{
    var clave = prompt("ingrese el número clave.");
    
    while(clave != "utn750")
    {
        if (clave == null)
            break;

        clave = prompt("Error, reingrese la clave.");
    }

}//FIN DE LA FUNCIÓN
